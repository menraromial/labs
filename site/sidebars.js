// La barre latérale suit l'arborescence générée dans docs/ :
// progression, expériences (une catégorie par dossier experiments/NN-nom), méthode.

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  laboratoire: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;
